---
it:
  activerecord:
    errors:
      models:
        webhook_endpoint:
          attributes:
            base:
              exceeded_limit: È stato raggiunto il numero massimo di endpoint webhook
