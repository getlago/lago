# pins-allow.txt - REVIEWED BASELINE of pre-existing floating versions.
#
# These were already in the repo before the pinning gate existed. They are an
# explicitly-accepted baseline: the gate prints each one as [BASE] on every run
# (never hidden) but does NOT fail on them, even under STRICT. Any NEW floating
# version (not listed here) always FAILS the gate, including in normal mode.
#
# Goal: drive this file to empty. When you pin one to an exact, validated version,
# delete its line here. (Locally-built dev images like *_dev are exempt
# automatically - they have no registry version - so they are not listed.)
#
# Format: one exact violation "key" per line (no surrounding quotes).

# --- production / deploy (pin these to validated versions before go-live) ----
# Portainer admin UI in the production stack.
compose:deploy/docker-compose.production.yml:portainer/portainer-ce:latest
# Quickstart helper script runs the all-in-one image at :latest.
sh:deploy/deploy.sh:getlago/lago:latest
# Production image build pulls newest pnpm -> pin to match front's packageManager.
docker:docker/Dockerfile:@latest:pnpm@latest

# --- local dev stack only (docker-compose.dev.yml; never deployed) -----------
compose:docker-compose.dev.yml:docker.redpanda.com/redpandadata/connectors:latest
compose:docker-compose.dev.yml:ankane/pghero:latest
compose:docker-compose.dev.yml:mailhog/mailhog
