import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { useMemo, useState } from "react";
import { api, duration, ms, num, pct, } from "../lib/api";
import { Banner, Card, Checklist, ClockPanel, ErrorsPanel, LogPanel, PercentileTable, Stat } from "../components/panels";
import { Funnel, Histogram, HopWaterfall, PercentileBars, Throughput } from "../components/charts";
const STAGE_LABELS = {
    rwEnriched: "RisingWave events_enriched",
    rwExpanded: "RisingWave events_expanded",
    chRwEnriched: "ClickHouse RW shadow (enriched)",
    chRwExpanded: "ClickHouse RW shadow (expanded)",
    chGoEnriched: "ClickHouse Go path (enriched)",
    chGoExpanded: "ClickHouse Go path (expanded)",
};
const ALL_STAGES = Object.keys(STAGE_LABELS);
const RUNNING = ["preflight", "sending", "draining"];
export function Run({ segments, spec, setSpec, discovery, snap, connected, }) {
    const [starting, setStarting] = useState(false);
    const [error, setError] = useState(null);
    const [histSeg, setHistSeg] = useState("ch_rw_expanded_visible");
    const live = snap && RUNNING.includes(snap.phase);
    const stats = snap?.stats ?? {};
    const unavailable = snap?.unavailable ?? [];
    // The run being displayed decides what its segments mean: on a direct-produce
    // run two of them stop describing Lago at all. The override lives in the
    // server's catalog, so the dashboard still cannot claim more than the catalog
    // says — it only picks which of the two statements applies.
    // A snapshot is authoritative about its own run — including a run persisted
    // before this transport existed, whose spec carries no transport at all and is
    // therefore an API run, whatever the form is currently set to.
    const direct = snap?.spec ? snap.spec.send.transport === "kafka" : spec.send.transport === "kafka";
    const shown = useMemo(() => direct
        ? segments.map((seg) => (seg.whenDirectProduce ? { ...seg, ...seg.whenDirectProduce } : seg))
        : segments, [segments, direct]);
    const polled = shown.filter((s) => s.kind === "polled");
    const stamped = shown.filter((s) => s.kind === "stamped");
    const start = async () => {
        setStarting(true);
        setError(null);
        try {
            await api.startRun(spec);
        }
        catch (e) {
            const body = e.body;
            setError(body?.error ?? e.message);
        }
        finally {
            setStarting(false);
        }
    };
    const stop = async () => {
        try {
            await api.stopRun();
        }
        catch (e) {
            setError(e.message);
        }
    };
    const estSeconds = spec.rateEps > 0 ? Math.round(spec.totalEvents / spec.rateEps) : 0;
    const headlineRows = useMemo(() => polled.flatMap((s) => {
        const p = stats[s.key];
        if (!p)
            return [];
        const row = { key: s.key, label: s.label, p50: p.p50, p95: p.p95, p99: p.p99, count: p.count };
        if (s.key === "usage_visible")
            row.note = snap?.usageMode === "watermark" ? "watermark attribution" : "exact probe";
        if (s.key === "wallet_visible")
            row.note =
                snap?.walletMode === "refresh"
                    ? "per refresh — upper bound"
                    : snap?.walletMode === "watermark"
                        ? "watermark attribution"
                        : "exact probe";
        if (s.key === "usage_to_wallet")
            row.note = "the wallet hop alone";
        return [row];
    }), [polled, stats, snap?.usageMode, snap?.walletMode]);
    const hops = useMemo(() => [
        { key: "ingest_to_broker", label: direct ? "this app → Redpanda" : "Lago → Redpanda" },
        { key: "broker_to_rw", label: "Redpanda → RisingWave" },
        { key: "rw_to_ch", label: "RisingWave → ClickHouse" },
    ]
        .map((h) => ({ ...h, value: stats[h.key]?.p50 ?? NaN }))
        .filter((h) => Number.isFinite(h.value)), [stats, direct]);
    const funnel = useMemo(() => {
        const out = [];
        if (snap?.counters)
            out.push({
                key: "accepted",
                label: direct ? "Acked by Redpanda" : "Accepted by the Lago API",
                count: snap.counters.accepted,
            });
        for (const s of ALL_STAGES) {
            const c = snap?.stageCounts?.[s];
            if (c != null)
                out.push({ key: s, label: STAGE_LABELS[s], count: c });
        }
        return out;
    }, [snap, direct]);
    /** A negative duration cannot happen physically: it means the two clocks disagree. */
    const skewed = useMemo(() => stamped.filter((seg) => (stats[seg.key]?.p50 ?? 0) < 0).map((seg) => seg.label), [stamped, stats]);
    const effectiveRate = useMemo(() => {
        const r = snap?.rate ?? [];
        if (r.length < 3)
            return null;
        const window = r.slice(-6);
        return Math.round(window.reduce((s, x) => s + x.sent, 0) / window.length);
    }, [snap]);
    return (_jsxs(_Fragment, { children: [error && _jsx(Banner, { kind: "bad", children: error }), !connected && _jsx(Banner, { kind: "warn", children: "Live stream disconnected \u2014 retrying. The server keeps running the test." }), direct && (_jsxs(Banner, { kind: "info", children: [_jsx("b", { children: "Direct produce." }), " Events are written straight to the raw events topic, byte-shape identical to what the API produces \u2014 so the Lago API is not in the send path and its ingest cost is ", _jsx("em", { children: "not" }), " part of any number below. \"Produce ack\" replaces \"API response\", and ", _jsx("code", { children: "ingested_at" }), " is stamped by this app rather than by Lago. The read paths measured here (current usage, wallets) still go through the API."] })), _jsxs(Card, { title: "Launch", hint: live ? "a run is in progress" : `${spec.targetIds.length} target(s) selected`, right: live ? (_jsxs("div", { className: "row", children: [_jsxs("span", { className: "pill", children: [_jsx("span", { className: "dot live" }), " ", snap?.phase] }), _jsx("button", { className: "btn danger", onClick: stop, children: "Stop" })] })) : (_jsx("button", { className: "btn primary", onClick: start, disabled: starting || spec.targetIds.length === 0, children: starting ? "Starting…" : "Run preflight & start" })), children: [spec.targetIds.length === 0 && (_jsx(Banner, { kind: "warn", children: "No targets selected. Pick some on the Targets tab first." })), _jsxs("div", { className: "grid cols-4", style: { marginTop: spec.targetIds.length === 0 ? 12 : 0 }, children: [_jsxs("label", { className: "field", children: ["Target rate (events/s)", _jsx("input", { type: "number", min: 1, value: spec.rateEps, disabled: !!live, onChange: (e) => setSpec({ ...spec, rateEps: Number(e.target.value) }) }), _jsx("span", { className: "note", children: spec.send.transport === "kafka"
                                            ? `${Math.ceil(spec.rateEps / Math.max(1, spec.send.batchSize))} produce/s of ${spec.send.batchSize} events`
                                            : spec.send.batchSize > 1
                                                ? `${Math.ceil(spec.rateEps / spec.send.batchSize)} POST/s of ${spec.send.batchSize} events`
                                                : "one POST per event" })] }), _jsxs("label", { className: "field", children: ["Total events", _jsx("input", { type: "number", min: 1, value: spec.totalEvents, disabled: !!live, onChange: (e) => setSpec({ ...spec, totalEvents: Number(e.target.value) }) }), _jsxs("span", { className: "note", children: ["\u2248 ", duration(estSeconds * 1000), " at the target rate"] })] }), _jsxs("label", { className: "field", children: ["Probe every Nth event", _jsx("input", { type: "number", min: 0, value: spec.probeEvery, disabled: !!live, onChange: (e) => setSpec({ ...spec, probeEvery: Number(e.target.value) }) }), _jsx("span", { className: "note", children: "0 disables visibility polling (stamps only)" })] }), _jsxs("label", { className: "field", children: ["Stop above error rate (%)", _jsx("input", { type: "number", min: 0, value: spec.guards.maxErrorRatePct, disabled: !!live, onChange: (e) => setSpec({ ...spec, guards: { ...spec.guards, maxErrorRatePct: Number(e.target.value) } }) }), _jsx("span", { className: "note", children: "judged over a trailing 10s window; 0 disables the stop" })] }), _jsxs("label", { className: "field", children: ["Hard cap (events)", _jsx("input", { type: "number", min: 1, value: spec.guards.hardCap, disabled: !!live, onChange: (e) => setSpec({ ...spec, guards: { ...spec.guards, hardCap: Number(e.target.value) } }) }), _jsx("span", { className: "note", children: "preflight refuses a run larger than this" })] })] }), _jsxs("div", { className: "row", style: { marginTop: 12, gap: 18 }, children: [_jsx("span", { style: { fontSize: 12, color: "var(--text-muted)" }, children: "Send:" }), _jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: ["transport", _jsxs("select", { style: { width: 190 }, value: spec.send.transport, disabled: !!live, onChange: (e) => {
                                            const transport = e.target.value;
                                            // The two transports have completely different useful batch
                                            // sizes (Lago caps at 100, a produce request does not), so
                                            // switching without moving the batch would either leave 5 000
                                            // events on a POST or 1 event on a produce request.
                                            const batchSize = transport === "kafka" ? Math.max(spec.send.batchSize, 500) : 1;
                                            setSpec({ ...spec, send: { ...spec.send, transport, batchSize } });
                                        }, children: [_jsx("option", { value: "api", children: "Lago API (POST /events)" }), _jsx("option", { value: "kafka", children: "Redpanda (direct produce)" })] })] }), _jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: ["events per ", spec.send.transport === "kafka" ? "produce" : "POST", _jsx("input", { type: "number", min: 1, max: spec.send.transport === "kafka" ? 2000 : 100, style: { width: 80 }, value: spec.send.batchSize, disabled: !!live, onChange: (e) => setSpec({ ...spec, send: { ...spec.send, batchSize: Number(e.target.value) } }) })] }), _jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: ["requests in flight", _jsx("input", { type: "number", min: 0, style: { width: 70 }, value: spec.send.maxInFlight, disabled: !!live, onChange: (e) => setSpec({ ...spec, send: { ...spec.send, maxInFlight: Number(e.target.value) } }) })] }), _jsx("span", { style: { fontSize: 12, color: "var(--text-muted)" }, children: spec.send.transport === "kafka"
                                    ? "Direct produce writes the API's own message to the raw events topic, so the send rate is no longer capped by Lago's round trip. The read paths (usage, wallets) still go through the API. 0 in flight = sized from the target rate and the ack latency being measured."
                                    : "0 = sized from the target rate and the round trip being measured. 1 event per POST is what a per-event integration sends; up to 100 is POST /events/batch." })] }), _jsxs("div", { className: "row", style: { marginTop: 12, gap: 18 }, children: [_jsx("span", { style: { fontSize: 12, color: "var(--text-muted)" }, children: "Spread:" }), _jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: ["values per group key", _jsx("input", { type: "number", min: 1, style: { width: 70 }, value: spec.spread.groupKeyValues, disabled: !!live, onChange: (e) => setSpec({ ...spec, spread: { ...spec.spread, groupKeyValues: Number(e.target.value) } }) })] }), _jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: [_jsx("input", { type: "checkbox", checked: spec.spread.includeDefaultBucket, disabled: !!live, onChange: (e) => setSpec({ ...spec, spread: { ...spec.spread, includeDefaultBucket: e.target.checked } }) }), "also hit the default bucket"] }), _jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: ["max shapes / target", _jsx("input", { type: "number", min: 1, style: { width: 70 }, value: spec.spread.maxVariantsPerTarget, disabled: !!live, onChange: (e) => setSpec({ ...spec, spread: { ...spec.spread, maxVariantsPerTarget: Number(e.target.value) } }) })] })] }), _jsxs("div", { className: "row", style: { marginTop: 12, gap: 18 }, children: [_jsxs("label", { className: "row", style: { fontSize: 13, gap: 6 }, children: [_jsx("input", { type: "checkbox", checked: spec.ramp.enabled, disabled: !!live, onChange: (e) => setSpec({ ...spec, ramp: { ...spec.ramp, enabled: e.target.checked } }) }), "Ramp"] }), spec.ramp.enabled && (_jsxs(_Fragment, { children: [_jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: ["from", _jsx("input", { type: "number", style: { width: 90 }, value: spec.ramp.fromEps, disabled: !!live, onChange: (e) => setSpec({ ...spec, ramp: { ...spec.ramp, fromEps: Number(e.target.value) } }) }), "/s"] }), _jsxs("label", { className: "row", style: { fontSize: 12, gap: 6 }, children: ["over", _jsx("input", { type: "number", style: { width: 90 }, value: spec.ramp.overSec, disabled: !!live, onChange: (e) => setSpec({ ...spec, ramp: { ...spec.ramp, overSec: Number(e.target.value) } }) }), "s"] })] })), _jsx("div", { style: { width: 1, height: 20, background: "var(--border)" } }), _jsx("span", { style: { fontSize: 12, color: "var(--text-muted)" }, children: "Stages to poll:" }), ALL_STAGES.map((s) => (_jsxs("label", { className: "row", style: { fontSize: 12, gap: 5 }, children: [_jsx("input", { type: "checkbox", checked: spec.stages[s], disabled: !!live, onChange: (e) => setSpec({ ...spec, stages: { ...spec.stages, [s]: e.target.checked } }) }), STAGE_LABELS[s]] }, s)))] })] }), snap?.preflight && snap.preflight.length > 0 && (_jsx(Card, { title: "Preflight", hint: "what is reachable, and therefore what this run can and cannot measure", children: _jsx(Checklist, { checks: snap.preflight }) })), snap && snap.phase !== "idle" && (_jsxs(_Fragment, { children: [_jsx(Card, { title: "Run", hint: snap.id, right: _jsxs("span", { className: "pill", children: [live ? _jsx("span", { className: "dot live" }) : _jsx("span", { className: `dot ${snap.phase === "done" ? "ok" : "warn"}` }), snap.phase] }), children: _jsxs("div", { className: "grid cols-4", children: [_jsx(Stat, { label: "Accepted", value: num(snap.counters?.accepted), sub: `of ${num(snap.counters?.sent)} sent · ${pct(snap.counters?.accepted ?? 0, snap.counters?.sent ?? 0)}` }), _jsx(Stat, { label: "Failed", value: num(snap.counters?.failed), sub: snap.counters?.sent ? pct(snap.counters.failed, snap.counters.sent) : "—" }), _jsx(Stat, { label: "Elapsed", value: duration(snap.elapsedMs), sub: effectiveRate != null ? `${effectiveRate}/s observed` : "—" }), _jsx(Stat, { label: snap.usageMode === "watermark" ? "Usage attributed" : "Probes in flight", value: snap.usageMode === "watermark"
                                        ? `${num(snap.probeTarget?.attributed)} / ${num(snap.probeTarget?.expected)}`
                                        : num(snap.counters?.pendingProbes), sub: `${num(snap.counters?.probes)} visibility probe(s) · ${num(snap.counters?.usageProbes)} usage sample(s)${snap.counters?.usageTimeouts ? ` · ${snap.counters.usageTimeouts} never counted` : ""}${snap.walletMode !== "off" ? ` · ${num(snap.counters?.walletProbes)} wallet sample(s)` : ""}${snap.counters?.walletTimeouts ? ` · ${snap.counters.walletTimeouts} never reached the wallet` : ""}` })] }) }), skewed.length > 0 && (_jsxs(Banner, { kind: "warn", children: [_jsxs("b", { children: ["Negative durations on ", skewed.length, " stamped segment(s)"] }), " \u2014 ", skewed.join("; "), ". A hop cannot take less than zero time, so this is clock disagreement between the two machines, not latency. Check the clock offsets panel below; the polled end-to-end numbers are unaffected because both of their endpoints come from this app's clock."] })), snap.usageFreshness && snap.usageFreshness.verdict === "batched" && (_jsxs(Banner, { kind: "bad", children: [_jsx("b", { children: "The usage numbers below are not per-event latency." }), " One reading accounted for", " ", num(snap.usageFreshness.worstBatch), " events at once (", Math.round(snap.usageFreshness.batchShare * 100), "% of the run), which is what a ", _jsx("em", { children: "cached" }), " read path looks like: it does not move while events arrive, then jumps once when something invalidates it. Every \"latency\" in that jump is really the same refresh, measured from each event's own send time \u2014 which is why the distribution comes out as a straight ramp. Fix the read path (", _jsx("code", { children: "LAGO_RISINGWAVE_USAGE_ENABLED=true" }), ", and the charge must be count/sum, in arrears, non-prorated, non-recurring, no custom expression), then re-run."] })), snap.usageFreshness && snap.usageFreshness.verdict === "coarse" && (_jsxs(Banner, { kind: "warn", children: ["Usage readings advanced in steps of up to ", num(snap.usageFreshness.worstBatch), " events, so the usage percentiles are coarser than the other stages. Poll harder (Setup \u2192 usage poll interval / in flight) or lower the event rate for a cleaner usage measurement."] })), snap.usageMode !== "off" && snap.usagePoll && (_jsx(Card, { title: "Usage sampling", hint: `${snap.usageMode} attribution · what the current_usage measurement can resolve`, children: _jsxs("div", { className: "grid cols-4", children: [_jsx(Stat, { label: "Polls / second", value: snap.usagePoll.perSecond.toFixed(1), sub: `${num(snap.usagePoll.completed)} completed · ${num(snap.usagePoll.inFlight)} in flight${snap.usagePoll.failed ? ` · ${num(snap.usagePoll.failed)} failed` : ""}` }), _jsx(Stat, { label: "current_usage RTT", value: ms(snap.usagePoll.rttP50), sub: `p95 ${ms(snap.usagePoll.rttP95)} — pipelined, so this does not set the resolution` }), _jsx(Stat, { label: "Uncertainty", value: `±${ms(snap.usagePoll.resolutionMs)}`, sub: `half the window each crossing was pinned inside (p95 window ${ms(snap.usagePoll.bracketP95Ms)})` }), _jsx(Stat, { label: "Attributed", value: `${num(snap.probeTarget?.attributed)} / ${num(snap.probeTarget?.expected)}`, sub: `by expected units on ${snap.probeTarget?.metric ?? "—"}` }), _jsx(Stat, { label: "Reading advance", value: snap.usageFreshness?.verdict === "incremental"
                                        ? "incremental"
                                        : snap.usageFreshness?.verdict ?? "unknown", sub: `largest single step ${num(snap.usageFreshness?.worstBatch)} event(s)${snap.usageFreshness?.batches ? ` · ${num(snap.usageFreshness.batches)} multi-event step(s)` : ""}` })] }) })), snap.walletFreshness?.staleAtStart && (_jsxs(Banner, { kind: "bad", children: [_jsx("b", { children: "The wallet numbers below are not latency." }), " Preflight sent one event and the ongoing balance never moved, so the refresh path was not running when this run started. Check that karafka is consuming", " ", _jsx("code", { children: "wallet_refresh_triggers" }), " (", _jsx("code", { children: "LAGO_KAFKA_WALLET_REFRESH_TRIGGERS_TOPIC" }), "), that the RisingWave ", _jsx("code", { children: "wallet_refresh_triggers_sink" }), " exists, and that ", _jsx("code", { children: "current_usage" }), " itself is live \u2014 the refresh reads usage, so a dead usage path is a dead wallet path."] })), snap.walletMode === "refresh" && (_jsxs(Banner, { kind: "warn", children: [_jsx("b", { children: "Wallet latency is an upper bound in this run." }), " The wallet's customer carries bulk traffic and at least one shape is not a ", _jsx("code", { children: "standard" }), " charge, so per-event cents are not predictable and each refresh is timed against the oldest outstanding event. A refresh covers every event whose bucket had landed, so the events behind it are charged to the next refresh rather than to the one that actually included them. Point both probes at the same standard charge for per-event truth."] })), snap.walletMode !== "off" && snap.walletFreshness?.verdict === "batched" && !snap.walletFreshness.staleAtStart && (_jsxs(Banner, { kind: "warn", children: ["One wallet reading accounted for ", num(snap.walletFreshness.worstBatch), " events at once (", Math.round(snap.walletFreshness.batchShare * 100), "% of the run). That is the consumer's batch collapse doing its job \u2014 N triggers for one customer cost one refresh \u2014 but it means the wallet percentiles describe refresh cadence rather than per-event work. Lower the rate to separate the two."] })), snap.walletMode !== "off" && snap.walletPoll && (_jsx(Card, { title: "Wallet sampling", hint: `${snap.walletMode} attribution · ${snap.walletProbe?.customer ?? "—"} · ${num(snap.walletProbe?.wallets)} active wallet(s)`, children: _jsxs("div", { className: "grid cols-4", children: [_jsx(Stat, { label: "Polls / second", value: snap.walletPoll.perSecond.toFixed(1), sub: `${num(snap.walletPoll.completed)} completed · ${num(snap.walletPoll.inFlight)} in flight${snap.walletPoll.failed ? ` · ${num(snap.walletPoll.failed)} failed` : ""}` }), _jsx(Stat, { label: "GET /wallets RTT", value: ms(snap.walletPoll.rttP50), sub: `p95 ${ms(snap.walletPoll.rttP95)} — the reading is a stored column, so polling never triggers the refresh it times` }), _jsx(Stat, { label: "Uncertainty", value: `±${ms(snap.walletPoll.resolutionMs)}`, sub: `half the window each crossing was pinned inside (p95 window ${ms(snap.walletPoll.bracketP95Ms)})` }), _jsx(Stat, { label: "Attributed", value: `${num(snap.walletProbe?.attributed)} / ${num(snap.walletProbe?.expected)}`, sub: snap.walletMode === "watermark"
                                        ? `by predicted cents × ${snap.walletProbe?.centsFactor ?? 1} calibration`
                                        : "by observed increase of the ongoing balance" }), _jsx(Stat, { label: "Refreshes", value: num(snap.walletProbe?.refreshes), sub: snap.walletProbe?.eventsPerRefresh != null
                                        ? `${snap.walletProbe.eventsPerRefresh} event(s) per refresh — the consumer's batch collapse${snap.walletProbe.refreshesExact ? "" : " (lower bound: counted from amount changes only)"}`
                                        : "none observed yet" }), _jsx(Stat, { label: "Reading advance", value: snap.walletFreshness?.verdict ?? "unknown", sub: `largest single step ${num(snap.walletFreshness?.worstBatch)} event(s)${snap.walletFreshness?.batches ? ` · ${num(snap.walletFreshness.batches)} multi-event step(s)` : ""}` }), _jsx(Stat, { label: "Calibration", value: snap.walletProbe?.canary || "—", sub: "one real event, measured at preflight \u2014 absorbs taxes, currency subunit and the wallet rate" }), _jsx(Stat, { label: "Usage \u2192 wallet split", value: snap.walletProbe?.aligned ? "per event" : "not measurable", sub: snap.walletProbe?.aligned
                                        ? "both probes cover the same events, so the gap is one event compared with itself"
                                        : "the probes cover different traffic — only the two distributions are comparable" })] }) })), _jsxs(Card, { title: "End-to-end latency, by stage", hint: "from the moment the POST left this app until a reader could see the row \u2014 single clock, no skew", children: [_jsx(PercentileBars, { rows: headlineRows }), headlineRows.length > 0 && (_jsxs("p", { style: { marginTop: 10, fontSize: 12, color: "var(--text-muted)" }, children: ["Resolution is the poll tick, so a value below it means \"seen on the first poll\". Sampled from every", " ", spec.probeEvery, "th event; the table below covers every event for the stamped segments."] }))] }), _jsxs("div", { className: "grid cols-2", children: [_jsxs(Card, { title: "Where the time goes", hint: "median hop, from timestamps the pipeline recorded (all events)", children: [_jsx(HopWaterfall, { hops: hops }), hops.length < 3 && (_jsx("p", { style: { marginTop: 8, fontSize: 12, color: "var(--text-muted)" }, children: "Missing hops are legs whose clock column does not exist upstream \u2014 see Preflight." }))] }), _jsx(Card, { title: "Did everything arrive?", hint: "distinct events of this run reaching each stage", children: _jsx(Funnel, { stages: funnel }) })] }), _jsx(Card, { title: "Distribution", hint: "the shape behind the percentiles \u2014 a long right tail and a shifted bulk mean different things", right: _jsx("select", { value: histSeg, onChange: (e) => setHistSeg(e.target.value), style: { width: 340 }, children: shown
                                .filter((s) => snap.histograms?.[s.key])
                                .map((s) => (_jsx("option", { value: s.key, children: s.label }, s.key))) }), children: _jsx(Histogram, { hist: snap.histograms?.[histSeg], p50: stats[histSeg]?.p50, p95: stats[histSeg]?.p95, p99: stats[histSeg]?.p99, label: shown.find((s) => s.key === histSeg)?.label ?? histSeg }) }), _jsx(Card, { title: "Throughput", hint: "what was actually achieved per second, against what was asked for", children: _jsx(Throughput, { series: snap.rate ?? [] }) }), _jsx(Card, { title: "All segments", hint: "the table view \u2014 polled segments are single-clock; stamped segments span two machines", children: _jsx(PercentileTable, { segments: [...polled, ...stamped], stats: stats, unavailable: unavailable }) }), _jsxs("div", { className: "grid cols-2", children: [_jsx(Card, { title: "Clock offsets", hint: "measured at preflight", children: _jsx(ClockPanel, { clocks: snap.clocks }) }), _jsx(Card, { title: "Errors", hint: "grouped by message", children: _jsx(ErrorsPanel, { errors: snap.errors ?? [] }) })] }), snap.spread && snap.spread.length > 0 && (_jsx(Card, { title: "Event spread", hint: `${snap.spread.length} shape(s) — every charge filter, the default bucket, and each pricing group key value${snap.spreadTruncated ? ` · ${num(snap.spreadTruncated)} capped` : ""}`, children: _jsx("div", { className: "scroll", children: _jsxs("table", { className: "data", children: [_jsx("thead", { children: _jsxs("tr", { children: [_jsx("th", { children: "Target" }), _jsx("th", { style: { textAlign: "left" }, children: "Shape" }), _jsx("th", { style: { textAlign: "left" }, children: "Bucket" }), _jsx("th", { style: { textAlign: "left" }, children: "Properties sent" }), _jsx("th", { children: "Events" })] }) }), _jsx("tbody", { children: snap.spread.map((v) => (_jsxs("tr", { children: [_jsx("td", { className: "mono", style: { fontSize: 12 }, children: v.target }), _jsx("td", { style: { textAlign: "left" }, children: v.label }), _jsx("td", { style: { textAlign: "left" }, children: _jsxs("span", { className: "pill", style: { fontSize: 11 }, children: [v.kind === "filter" ? "charge filter" : "default", v.grouped ? " · grouped" : ""] }) }), _jsx("td", { className: "mono", style: { textAlign: "left", fontSize: 11, color: "var(--text-secondary)" }, children: Object.entries(v.properties)
                                                        .map(([k, val]) => `${k}=${val}`)
                                                        .join(" ") || "—" }), _jsx("td", { className: "num", children: num(v.sent) })] }, v.target + v.label))) })] }) }) })), _jsx(Card, { title: "Log", hint: "newest first", children: _jsx(LogPanel, { logs: snap.logs }) })] })), (!snap || snap.phase === "idle") && (_jsx(Card, { title: "What this measures", hint: "read once, then the numbers above interpret themselves", children: _jsxs("div", { className: "grid cols-2", children: [_jsxs("div", { children: [_jsx("h3", { children: "Polled \u2014 the trustworthy end-to-end numbers" }), _jsx("p", { style: { fontSize: 13, color: "var(--text-secondary)", marginTop: 6 }, children: "A sampled event is queried at each stage every poll tick until it appears. Both ends of the measurement \u2014 the moment the POST left, and the tick that first saw the row \u2014 are read from this app's clock, so no cross-cloud clock skew can enter. Cost is flat in the number of probes: one query per stage per tick for the whole in-flight set, not one per event." })] }), _jsxs("div", { children: [_jsx("h3", { children: "Stamped \u2014 the per-hop breakdown" }), _jsxs("p", { style: { fontSize: 13, color: "var(--text-secondary)", marginTop: 6 }, children: ["Every event (not just probes) also carries the timestamps the pipeline itself recorded:", " ", _jsx("code", { children: "ingested_at" }), " from Lago, ", _jsx("code", { children: "kafka_timestamp" }), " from the broker,", " ", _jsx("code", { children: "rw_received_at" }), " from RisingWave, ", _jsx("code", { children: "enriched_at" }), " from ClickHouse. These pinpoint the expensive hop, but each spans two machines' clocks \u2014 the offsets panel shows how much that is worth."] })] })] }) }))] }));
}
